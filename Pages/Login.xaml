<Page x:Class="WpfApp40.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp40.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Login">
    <Page.Resources>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="18"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="18"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Margin" Value="0 50"/>
            <Setter Property="Padding" Value="0 10"/>
        </Style>
    </Page.Resources>
    <Grid>
        <StackPanel MinWidth="300" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label Content="Имя:"/>
            <TextBox x:Name="tbName" TextChanged="tbName_TextChanged"/>
            <Label Content="Пароль:"/>
            <TextBox x:Name="tbPass" TextChanged="tbName_TextChanged"/>
            <Button x:Name="btnSend"  Content="Войти" Click="btnSend_Click"/>
        </StackPanel>
    </Grid>
</Page>
